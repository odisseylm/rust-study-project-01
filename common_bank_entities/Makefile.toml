extend = { path = "../build/common-makefile.toml" }

[env]
PROJECT_DIR = "${CARGO_MAKE_WORKSPACE_WORKING_DIRECTORY}/common_bank_entities"

[tasks.test]
command = "true" # I cannot just use `extend = "cargo-test"`. It does not work.
args = []
dependencies = [ "cargo-test" ]
