
fn main() {
    // use std::env;
    // env::set_var("RUSTC_VERSION_EX", version_check::triple().unwrap().to_string());
    // let aaa = version_check::triple().unwrap();
    // env::set_var("RUSTC_VERSION_EX", format!("{} - {} - {}", aaa.0, aaa.1, aaa.2));
    // env::set_var("RUSTC_VERSION", rustc_version::version().unwrap().to_string());
    // panic!("RUSTC_VERSION: {}", rustc_version::version().unwrap().to_string())
}